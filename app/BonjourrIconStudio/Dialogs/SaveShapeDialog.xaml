<Window x:Class="BonjourrIconStudio.Dialogs.SaveShapeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сохранить форму"
        Width="430" Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Название формы" Style="{StaticResource SectionTitleText}" />
        <TextBox x:Name="NameTextBox" Grid.Row="1" Margin="0,14,0,0" MaxLength="48" />
        <TextBlock x:Name="ValidationText" Grid.Row="2" Margin="0,8,0,0" Foreground="{StaticResource DangerBrush}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="100" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Сохранить" Width="110" Style="{StaticResource PrimaryButton}" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
